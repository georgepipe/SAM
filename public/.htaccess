<Ifmodule mod_rewrite.c>
    Options -Multiviews
    RewriteEngine On
    #Make sure rewrite bass is set to project directory
    RewriteBase /SAM
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.+)$ index.php?url=$1 [L]
</IfModule>
   